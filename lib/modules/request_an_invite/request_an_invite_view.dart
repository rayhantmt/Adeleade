import 'package:flutter/material.dart';

class RequestAnInviteView extends StatelessWidget {
  const RequestAnInviteView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}