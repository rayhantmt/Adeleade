import 'package:flutter/material.dart';

class RequestAnInviteSecond extends StatelessWidget {
  const RequestAnInviteSecond({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}