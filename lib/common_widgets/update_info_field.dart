import 'package:flutter/material.dart';

class UpdateInfoField extends StatelessWidget {
  const UpdateInfoField({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}